用一个 Python 程序（ extract_features.py ），让 CLIP 模型把刚才下载的 5000 张 COCO 图片 全部“看”一遍，并把每张图都变成一个 512维的向量 保存下来。
保存的这个文件是整个毕设系统的“地基”。

运行结果： 两个进度条。
第一个是图片处理，大约需要 1-2 分钟（取决于显卡（RTX 3050 ）性能）。
第二个是文本处理，COCO 有 25000 句描述，可能需要几十秒。
结果： 运行结束后，你的文件夹里会多出一个 coco_features.pt 文件（大约 60-70MB）。